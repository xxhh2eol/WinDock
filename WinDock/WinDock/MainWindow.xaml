<Window x:Name="RootWindow" x:Class="WinDock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WinDock"
        xmlns:controls="clr-namespace:WinDock.Controls"
        Title="" Height="720" Width="1100"
        WindowStartupLocation="Manual"
        ShowActivated="True"
        ShowInTaskbar="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <local:DockIconConverter x:Key="DockIconConverter" />
        <local:HasNoteConverter x:Key="HasNoteConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="IconTileStyle" TargetType="Border">
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Width" Value="140" />
            <Setter Property="Height" Value="164" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Background" Value="#FFFFFFFF" />
            <Setter Property="BorderBrush" Value="#66FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <EventSetter Event="PreviewMouseRightButtonDown" Handler="Icon_PreviewRightButtonDown" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEAF4FF" />
                    <Setter Property="BorderBrush" Value="#8099C8F7" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="WindowButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="38" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="3,0,0,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="#FF475569" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#22007ACC" />
                    <Setter Property="Foreground" Value="#FF0078D4" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#FF64748B" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="16,9" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" Background="Transparent" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#FFEAFBFF" />
                                <Setter Property="Foreground" Value="#FF0067B8" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#220078D4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ItemsControl">
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UseVirtualization}" Value="True">
                    <Setter Property="ItemsPanel">
                        <Setter.Value>
                            <ItemsPanelTemplate>
                                <controls:VirtualizingWrapPanel />
                            </ItemsPanelTemplate>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border x:Name="WindowSurfaceBorder" CornerRadius="22" Background="#FFF6F9FC" BorderBrush="#80FFFFFF" BorderThickness="1" Padding="1">
        <Grid Background="Transparent" TextElement.FontFamily="{Binding AppFontFamily}" PreviewMouseLeftButtonDown="Root_PreviewMouseLeftButtonDown" PreviewMouseMove="Root_PreviewMouseMove" PreviewMouseLeftButtonUp="Root_PreviewMouseLeftButtonUp">
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0">
                    <Button Content="—" Style="{StaticResource WindowButtonStyle}" Click="Minimize_Click" />
                    <Button Content="×" Style="{StaticResource WindowButtonStyle}" Click="Close_Click" />
                </StackPanel>
            </Grid>
            <TabControl x:Name="MainTabs" Grid.Row="1" Margin="18,0,18,18" SelectedIndex="0" TextElement.FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}">
            <TabItem x:Name="DefaultTab" Header="默认" Tag="默认">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding DefaultItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource IconTileStyle}" Tag="{Binding}" MouseLeftButtonDown="Item_DoubleClick">
                                    <Border.LayoutTransform>
                                        <ScaleTransform ScaleX="{Binding DataContext.IconScale, RelativeSource={RelativeSource AncestorType=Window}}" ScaleY="{Binding DataContext.IconScale, RelativeSource={RelativeSource AncestorType=Window}}" />
                                    </Border.LayoutTransform>
                                    <StackPanel>
                                        <Image Height="64" Width="64" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" Opacity="{Binding DataContext.IconOpacity, RelativeSource={RelativeSource AncestorType=Window}}" Source="{Binding IconPath, Converter={StaticResource DockIconConverter}}" />
                                        <TextBlock Text="{Binding DisplayName}" FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,10,0,0" />
                                        <controls:MarqueeTextBlock Text="{Binding Note}" FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#FF94A3B8" Margin="0,6,0,0" MaxWidth="124" Visibility="{Binding Note, Converter={StaticResource HasNoteConverter}}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
            <TabItem x:Name="MoreTab" Header="更多" Tag="更多">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding MoreItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource IconTileStyle}" Tag="{Binding}" MouseLeftButtonDown="Item_DoubleClick">
                                    <Border.LayoutTransform>
                                        <ScaleTransform ScaleX="{Binding DataContext.IconScale, RelativeSource={RelativeSource AncestorType=Window}}" ScaleY="{Binding DataContext.IconScale, RelativeSource={RelativeSource AncestorType=Window}}" />
                                    </Border.LayoutTransform>
                                    <StackPanel>
                                        <Grid Height="64" Width="64">
                                            <Image Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" Opacity="{Binding DataContext.IconOpacity, RelativeSource={RelativeSource AncestorType=Window}}" Source="{Binding IconPath, Converter={StaticResource DockIconConverter}}" />
                                            <Border Background="#FFE11D48" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="4,1" Visibility="{Binding IsNew, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="新" Foreground="White" FontSize="10" />
                                            </Border>
                                        </Grid>
                                        <TextBlock Text="{Binding DisplayName}" FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,10,0,0" />
                                        <controls:MarqueeTextBlock Text="{Binding Note}" FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#FF94A3B8" Margin="0,6,0,0" MaxWidth="124" Visibility="{Binding Note, Converter={StaticResource HasNoteConverter}}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
            <TabItem x:Name="HiddenTab" Header="隐藏" Tag="隐藏">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding HiddenItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource IconTileStyle}" Tag="{Binding}" MouseLeftButtonDown="Item_DoubleClick">
                                    <Border.LayoutTransform>
                                        <ScaleTransform ScaleX="{Binding DataContext.IconScale, RelativeSource={RelativeSource AncestorType=Window}}" ScaleY="{Binding DataContext.IconScale, RelativeSource={RelativeSource AncestorType=Window}}" />
                                    </Border.LayoutTransform>
                                    <StackPanel>
                                        <Image Height="64" Width="64" Opacity="{Binding DataContext.HiddenIconOpacity, RelativeSource={RelativeSource AncestorType=Window}}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" Source="{Binding IconPath, Converter={StaticResource DockIconConverter}}" />
                                        <TextBlock Text="{Binding DisplayName}" FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,10,0,0" Foreground="#FF64748B" />
                                        <controls:MarqueeTextBlock Text="{Binding Note}" FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#FF94A3B8" Margin="0,6,0,0" MaxWidth="124" Visibility="{Binding Note, Converter={StaticResource HasNoteConverter}}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
            <TabItem x:Name="SettingsTab" Header="设置" Tag="设置">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="24" MaxWidth="720" TextElement.FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}">
                        <TextBlock Text="外观" FontSize="22" FontWeight="SemiBold" Foreground="#FF1E293B" />
                        <TextBlock Text="调整界面外观，设置会自动保存并在下次启动时恢复。" Foreground="#FF64748B" Margin="0,6,0,20" />

                        <CheckBox Content="窗口阴影（关闭可降低内存占用）" IsChecked="{Binding WindowShadow}" Margin="0,0,0,10" />
                        <CheckBox Content="图标列表虚拟化（优化内存与切换速度）" IsChecked="{Binding UseVirtualization}" Margin="0,0,0,16" />

                        <TextBlock Text="排序方式" Foreground="#FF475569" Margin="0,4,0,6" />
                        <ComboBox x:Name="SortModeComboBox" Width="240" Padding="8" Margin="0,0,0,20" SelectionChanged="SortModeComboBox_SelectionChanged">
                            <ComboBoxItem Content="默认（手动拖动排序）" Tag="Default" />
                            <ComboBoxItem Content="名称：A → Z" Tag="NameAsc" />
                            <ComboBoxItem Content="名称：Z → A" Tag="NameDesc" />
                            <ComboBoxItem Content="安装时间：新 → 旧" Tag="InstallDesc" />
                            <ComboBoxItem Content="安装时间：旧 → 新" Tag="InstallAsc" />
                        </ComboBox>

                        <!-- 字体修改功能暂时注释（保留代码，便于后续恢复）。
                        <TextBlock Text="字体" Foreground="#FF475569" />
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,18">
                            <ComboBox x:Name="FontFamilyComboBox" Width="260" Padding="8" VerticalAlignment="Center" SelectionChanged="FontFamilyComboBox_SelectionChanged" />
                            <TextBlock x:Name="FontPreviewText" Text="字体预览 AaBbCc 123 中文测试" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="#FF334155" FontSize="18" />
                        </StackPanel>
                        -->

                        <TextBlock Text="图标透明度" Foreground="#FF475569" />
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,18">
                            <Slider x:Name="IconOpacitySlider" Width="260" Minimum="0" Maximum="100" Value="100" ValueChanged="IconOpacitySlider_ValueChanged" VerticalAlignment="Center" />
                            <TextBlock x:Name="IconOpacityValueText" Text="100%" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#FF64748B" Width="48" />
                        </StackPanel>

                        <TextBlock Text="窗口透明度" Foreground="#FF475569" />
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,24">
                            <Slider x:Name="WindowOpacitySlider" Width="260" Minimum="30" Maximum="100" Value="100" ValueChanged="WindowOpacitySlider_ValueChanged" VerticalAlignment="Center" />
                            <TextBlock x:Name="WindowOpacityValueText" Text="100%" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#FF64748B" Width="48" />
                        </StackPanel>

                        <Separator Margin="0,4,0,20" />

                        <TextBlock Text="添加图标" FontSize="22" FontWeight="SemiBold" Foreground="#FF1E293B" />
                        <TextBlock Text="添加内容只会保存到 WinDock，不会修改真实文件或文件夹。" Foreground="#FF64748B" Margin="0,6,0,20" />
                        <TextBlock Text="目标 Tab" Foreground="#FF475569" />
                        <ComboBox x:Name="TargetGroupComboBox" SelectedIndex="0" Margin="0,6,0,18" Padding="8">
                            <ComboBoxItem Content="默认" Tag="Default" />
                            <ComboBoxItem Content="更多" Tag="More" />
                            <ComboBoxItem Content="隐藏" Tag="Hidden" />
                        </ComboBox>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="添加文件快捷方式" Padding="14,8" Margin="0,0,12,0" Click="AddFile_Click" />
                            <Button Content="添加文件夹" Padding="14,8" Margin="0,0,12,0" Click="AddFolder_Click" />
                            <Button Content="刷新图标列表" Padding="14,8" Click="RefreshList_Click" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            </TabControl>
        <StackPanel x:Name="IconSizePanel" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,4,34,0" Panel.ZIndex="10" Background="#FFF6F9FC" TextElement.FontFamily="{Binding DataContext.AppFontFamily, RelativeSource={RelativeSource AncestorType=Window}}">
            <TextBlock Text="图标大小" VerticalAlignment="Center" Foreground="#FF64748B" Margin="0,0,8,0" />
            <Slider x:Name="IconSizeSlider" Width="150" Minimum="24" Maximum="128" Value="64" ValueChanged="IconSizeSlider_ValueChanged" />
        </StackPanel>
        </Grid>
    </Border>
</Window>
